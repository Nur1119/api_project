# PROGRESS.md — Автотесты REST API (Capital One Nessie)

> **Для любого ИИ, который продолжает работу:** прочитай этот файл целиком.
> Ориентируйся на раздел **«Текущий статус»**. **Не перескакивай через незавершённые шаги.**
> После каждого выполненного шага: обнови чекбоксы, раздел «Текущий статус» и «Журнал», затем закоммить.
> Это единый источник правды по статусу. Карты контекста проекта — в `.agents/` (вход: `.agents/README.md`).

## О проекте
Учебно-практический проект по автоматизированному тестированию REST API банковского приложения. Тестируемое API — Capital One Nessie (sandbox). Формат гибридный: рефреш стека + реальная практика прямо в репозитории.

## Зачем / чему учит
- **Java 25** (core, коллекции, стримы) на практике
- **Maven** — сборка, зависимости, жизненный цикл
- **REST Assured** — API-тесты, сериализация, проверки
- **WireMock** — стабы, контрактное тестирование, изоляция от нестабильного API
- **Docker** — контейнеризация тест-окружения
- Best practices архитектуры автотестов + подготовка к собесам в финтехе

## Стек
Java 25 · Maven · JUnit 5 · REST Assured · Jackson · (Lombok) · WireMock · Docker · GitHub Actions (CI)

## Тестируемое API
Кратко: base `http://api.nessieisreal.com`, ключ query-параметром `?key=...`, ключ в `.env` (не в git). Полная карта — `.agents/rules/api-map.md`, термины — `.agents/rules/glossary.md`.

## Фазы
- [ ] **Фаза 1 — Окружение и каркас проекта**
  - [x] JDK 25 установлен (`java -version` показывает 25)
  - [x] IntelliJ IDEA установлена
  - [x] Git + GitHub-репозиторий, локальный клон
  - [x] Ключ Nessie получен и положен в env (не в репо)
  - [x] Каркас Maven-проекта: `pom.xml`, структура, `.gitignore`, Maven wrapper
  - [ ] Первый smoke-тест зелёный (`GET /accounts?key=...` → `200`)
- [ ] **Фаза 2 — REST Assured: базовые тесты Nessie** (GET-эндпоинты, статусы, тела, JSON path)
- [ ] **Фаза 3 — Модели домена + CRUD** (POJO/record, Jackson, create/read/update/delete)
- [ ] **Фаза 4 — Архитектура фреймворка** (request/response specs, конфиг, тест-данные, переиспользование)
- [ ] **Фаза 5 — WireMock** (стабы, матчинг запросов, контрактные тесты, изоляция от флакого Nessie)
- [ ] **Фаза 6 — Docker** (контейнеризация, WireMock в Docker, `docker-compose` тест-окружение)
- [ ] **Фаза 7 — CI и полировка** (GitHub Actions, уборка, подготовка к собесу)

## AI-инфраструктура (сквозная, вне фаз)
- [x] Контекст-слой `.agents/` (README-роутер + rules) + скилл `add-endpoint`. `CLAUDE.md` = loader с `@`-импортом. Заимствовано из монорепо sourcing-e2e.

## Текущий статус
**Фаза 1 — почти закрыта.**
- Сделано: JDK 25, IntelliJ, Git+GitHub, ключ в `.env` (проверен), каркас Maven (wrapper 3.9.2). **Заложена AI-инфра `.agents/`** (карты контекста + скилл), код очищен от комментов (знание → `.agents`), введено правило «апрув до реализации».
- **Каркас кода написан по слоям** (config/routes/specs/api/base/tests): `AccountsSmokeTest` через `AccountsApi` + `Specs`.
- **Сейчас:** шаг 6 — прогнать `AccountsSmokeTest` в IntelliJ на Java 25, убедиться в зелёном (`200` + JSON). После этого Фаза 1 закрыта.
- Дальше: Фаза 2 — базовые GET-тесты (рецепт добавления домена — `.agents/skills/add-endpoint/SKILL.md`).

## Журнал
- 2026-06-29 — Проект инициализирован. Заведён роадмап и трекер прогресса.
- 2026-06-29 — Стек переведён с Java 21 на **Java 25** (LTS). Установлен Temurin JDK 25.0.3, `JAVA_HOME`/`PATH` настроены. Чекбоксы JDK + IntelliJ закрыты.
- 2026-06-29 — Первый коммит, добавлен `.gitignore`. Запушено на GitHub. Шаг 3 закрыт.
- 2026-06-30 — Ключ Nessie в `.env` (gitignored), проверен `GET /accounts` → `200 []`. Шаг 4 закрыт.
- 2026-06-30 — Каркас Maven: `pom.xml` (Java 25, JUnit 5.14.4, REST Assured 6.0.0, compiler 3.15.0 + surefire 3.5.6), структура, Maven wrapper 3.9.2. `BUILD SUCCESS`. Шаг 5 закрыт.
- 2026-07-01 — Каркас кода по слоям (config/routes/specs/api/base/tests) + `AccountsSmokeTest`. Первая версия была плоской — переписана с разделением слоёв после ревью.
- 2026-07-01 — Внедрён AI-context слой `.agents/` (README-роутер + rules: project-map, architecture, conventions, source-code-safety, glossary, api-map) и скилл `add-endpoint`. Техника заимствована из монорепо sourcing-e2e (курируемая проза; генераторные каталоги отложены — YAGNI). `CLAUDE.md` слим до loader'а. Из кода убраны объяснительные комментарии. Введено правило «апрув до реализации».
